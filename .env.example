# Vite dev host (server.host). Default 127.0.0.1; set 0.0.0.0 to test from LAN.
WEI_HOST=127.0.0.1
# Extra hostnames Vite accepts (server.allowedHosts), comma-separated.
# Needed when opening dev over Tailscale/LAN names instead of IPs.
#WEI_ALLOWED_HOSTS=zen,zen.time-ostrich.ts.net
